var assert = require('assert'); 
var maybeFirst = require('../server.js');

describe('maybeFirst', function() {
  it('returns the first element of an array', function() {
    var result = "";

    assert.equal(result, "", 'expected empty'); //expected empty array when start
  });
});
